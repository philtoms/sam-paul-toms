---
import '../styles/global.css';
import SEOHead from '../components/SEOHead.astro';
import Player from '../components/AudioPlayer/Player';
import ContactModal from '../components/ContactModal';
import AboutModal from '../components/AboutModal';
import ProjectModal from '../components/ProjectModal';
import { DEFAULT_SITE_NAME } from '../scripts/seo-helpers';

interface PressQuote {
  text: string;
  source: string;
  url?: string;
}

interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalUrl?: string;
  type?: 'website' | 'music.song' | 'music.album' | 'music.playlist';
  structuredData?: object | object[];
  noindex?: boolean;
  aboutTitle?: string;
  aboutPhoto?: string;
  aboutPhotoAlt?: string;
  aboutGenreTags?: string[];
  aboutPressQuotes?: PressQuote[];
}

const siteName = import.meta.env.PUBLIC_SITE_NAME || DEFAULT_SITE_NAME;

const {
  title = siteName,
  description = `Official music portfolio of ${siteName} — listen to tracks, albums, and EPs.`,
  image,
  canonicalUrl,
  type = 'website',
  structuredData,
  noindex,
  aboutTitle = '',
  aboutPhoto = '',
  aboutPhotoAlt = '',
  aboutGenreTags = [],
  aboutPressQuotes = [],
} = Astro.props;

// Accent colour override from env var. Falls back to #eab308 (yellow-500).
// --color-accent-hover is computed via color-mix() at runtime.
const accentColor = import.meta.env.PUBLIC_ACCENT_COLOR || '#eab308';
---

<html lang="en" style={`--color-accent: ${accentColor}; --color-accent-hover: color-mix(in srgb, var(--color-accent) 80%, white); --color-accent-muted: var(--color-accent);`}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <SEOHead
      title={title}
      description={description}
      image={image}
      canonicalUrl={canonicalUrl}
      type={type}
      structuredData={structuredData}
      noindex={noindex}
      siteName={siteName}
    />
  </head>
  <body class="bg-bg text-text antialiased min-h-screen">
    <slot />
    <div transition:persist="audio-player" class="fixed bottom-0 z-50">
      <Player client:load />
    </div>
    <ContactModal client:load />
    <ProjectModal client:load />
    {aboutTitle && (
      <AboutModal
        client:load
        title={aboutTitle}
        photo={aboutPhoto}
        photoAlt={aboutPhotoAlt}
        genreTags={aboutGenreTags}
        pressQuotes={aboutPressQuotes}
      />
    )}
  </body>
</html>
